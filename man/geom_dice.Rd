% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/geom_dice.R
\name{geom_dice}
\alias{geom_dice}
\alias{GeomDice}
\title{A ggplot2 layer for creating dice representations}
\usage{
geom_dice(
  mapping = NULL,
  data = NULL,
  stat = "identity",
  position = "identity",
  show.legend = NA,
  inherit.aes = TRUE,
  na.rm = FALSE,
  dice_size = 1,
  dot_size = NULL,
  dice_color = "white",
  dice_alpha = 0.8,
  dot_stroke = 0.5,
  ...
)
}
\arguments{
\item{mapping}{Set of aesthetic mappings created by \code{aes()}. Must include:
\itemize{
\item \code{x}: x-axis position
\item \code{y}: y-axis position
\item \code{z}: number of dots on dice (1-6)
}

Optional aesthetics:
\itemize{
\item \code{colour}/\code{color}: color of the dice dots
\item \code{fill}: fill color of the dice dots (same as colour for dots)
\item \code{alpha}: transparency of the dice dots
\item \code{size}: size scaling factor
}}

\item{data}{A data frame. If \code{NULL}, the default, the data is inherited from the plot.}

\item{stat}{The statistical transformation to use on the data for this layer.}

\item{position}{Position adjustment, either as a string, or the result of a call to a position adjustment function.}

\item{show.legend}{logical. Should this layer be included in the legends?}

\item{inherit.aes}{If \code{FALSE}, overrides the default aesthetics.}

\item{na.rm}{If \code{FALSE}, the default, missing values are removed with a warning. If \code{TRUE}, missing values are silently removed.}

\item{dice_size}{Numeric. Size of the dice background rectangle. Default is 1.}

\item{dot_size}{Numeric. Size of the dots on the dice. If \code{NULL}, calculated as 20\% of \code{dice_size}.}

\item{dice_color}{Character. Background color of the dice. Default is "white".}

\item{dice_alpha}{Numeric. Transparency of the dice background. Default is 0.8.}

\item{dot_stroke}{Numeric. Stroke width for dot outlines. Default is 0.5.}

\item{...}{Other arguments passed on to \code{layer()}.}
}
\value{
A ggplot2 layer that can be added to a ggplot object.
}
\description{
\code{geom_dice()} creates a ggplot2 layer that displays dice representations in a plot.
Each dice can have 1-6 dots arranged in traditional dice patterns, with customizable
colors and sizes. This is useful for visualizing categorical data with multiple
dimensions in an intuitive and visually appealing way.
}
\section{Aesthetics}{

\code{geom_dice()} understands the following aesthetics (required aesthetics are in bold):
\itemize{
\item \strong{x}: x-axis position
\item \strong{y}: y-axis position
\item \strong{z}: number of dots (1-6)
\item \code{colour}/\code{color}: color of the dice dots
\item \code{fill}: fill color of the dice dots
\item \code{alpha}: transparency of the dice dots
\item \code{size}: size scaling factor
}
}

\section{Dice Patterns}{

The dice patterns follow traditional dice arrangements:
\itemize{
\item \strong{1}: Single dot in center
\item \strong{2}: Two dots on diagonal
\item \strong{3}: Three dots on diagonal
\item \strong{4}: Four dots in corners
\item \strong{5}: Four dots in corners + center dot
\item \strong{6}: Six dots in two columns
}
}

\examples{
library(ggplot2)
library(dplyr)

# Create sample data
data <- expand.grid(x = 1:3, y = 1:3) \%>\%
  mutate(
    z = sample(1:6, n(), replace = TRUE),
    category = sample(c("A", "B", "C"), n(), replace = TRUE)
  )

# Basic dice plot
ggplot(data, aes(x = x, y = y, z = z)) +
  geom_dice()

# Dice plot with colors
ggplot(data, aes(x = x, y = y, z = z, color = category)) +
  geom_dice(dice_size = 1.5)

# Custom styling
ggplot(data, aes(x = x, y = y, z = z)) +
  geom_dice(
    dice_size = 1.2,
    dice_color = "lightgray",
    dot_size = 0.4
  )

}
